import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { useActor } from './useActor';

// This file is reserved for additional React Query hooks if needed in the future
// Currently, feature-specific hooks are defined in their respective feature directories
